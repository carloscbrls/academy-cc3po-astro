<section class="hero">
  <div class="container">
    <div class="badge">Free courses available</div>
    <h1>CC3PO Academy</h1>
    <p class="tagline">Learn to protect, optimize, and grow your digital presence.<br/>No tech jargon. Just results.</p>
    <a href="#courses" class="cta">Explore Courses</a>
  </div>
</section>
<style>
  .hero { padding: 80px 0 60px; text-align: center; }
  .badge {
    display: inline-block; background: rgba(16,185,129,0.1); color: var(--accent);
    border: 1px solid rgba(16,185,129,0.2); border-radius: 999px;
    padding: 6px 16px; font-size: 0.8rem; font-weight: 600;
    margin-bottom: 24px;
  }
  h1 { font-size: clamp(2.5rem, 6vw, 4rem); font-weight: 800; letter-spacing: -0.03em; line-height: 1.1; }
  .tagline { color: var(--text-muted); font-size: 1.2rem; margin-top: 16px; max-width: 540px; margin-inline: auto; }
  .cta {
    display: inline-block; margin-top: 32px;
    background: var(--accent); color: #000; font-weight: 700;
    padding: 14px 32px; border-radius: 999px; font-size: 1rem;
    transition: background 0.2s;
  }
  .cta:hover { background: var(--accent-hover); }
</style>